{
  "name": "md2site",
  "version": "0.5.0",
  "description": "Converte arquivos Markdown em HTML+CSS em formato de site",
  "main": "./script/converter.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r public/*"
  },
  "author": "FabioLutz",
  "license": "ISC",
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
